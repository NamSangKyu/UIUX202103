position
    웹상에서 해당 요소를 고정하는 형태
    fixed, absolute, relative를 이용하여 고정
    top, left, right, bottom 값을 이용해서 원하는 곳에 배치

z-index : 레이어 개념으로 값이 높을 수록 앞에 출력, 낮을수록 뒷편에 배치
    z-index를 사용할려면 position 속성이 : static이면 안됨

